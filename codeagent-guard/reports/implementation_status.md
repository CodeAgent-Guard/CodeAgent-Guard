# 信安赛作品要求实现状态

## 已实现

- 自研轻量 AI Agent 与外部 LLM。
- Agent、Tool Proxy、Policy Engine、Tool Executor、Audit 和 Dashboard 模块化。
- 文件读写、命令、HTTP 和邮件五类基础工具监管，另扩展五类工具。
- 每任务工具白名单与 `tool_not_allowed`。
- Allow、Ask、Deny 三级决策和用户审批恢复/拒绝闭环。
- ASK 时暂停 Agent，会在用户批准或拒绝后恢复原会话并生成最终回答。
- 工具链完成后由 Agent 二次生成自然语言执行总结，并对邮件排队等关键状态做事实校验。
- 路径穿越、敏感文件、危险命令、远程脚本、反弹 Shell、SSRF、密钥外传和外部收件人检测。
- 不可信 README、代码注释、日志、配置文件和工具输出样本。
- 调用时间线、风险告警、动态控制链路、参数详情和攻击复现摘要。
- Agent 历史任务侧列表与完整 Trace 回放。
- 审计页按 Trace 会话分组、会话筛选和完整本地日期时间展示。
- SQLite 审计记录与 SHA-256 前向哈希链。
- SQLite Trace 持久化，服务重启后仍可查看历史任务。
- 连续上下文、历史对话、上下文容量提醒和每轮问答折叠。
- 用户可动态添加并持久保存可信工作环境。
- OpenCode `tool.execute.before` 执行前授权插件和工具语义映射。
- CT-TRM 来源建模、安全实体抽取、参数溯源和污染传播。
- 自动任务能力预算，不要求用户默认手动勾选工具。
- C1-C6 序列风险和 P1-P15 风险模式。
- 固定可解释风险评分、硬拒绝和 Decision 单调合并。
- Trace/Audit 中的 CT-TRM 结构化证据与 secret 脱敏。
- 500 条 AgentToolBench、六模式消融评测和 120 条红队绕过集。
- 隔离数据库副本篡改实验。
- 100 条策略回归测试。
- 无防护 ASR、防护阻断率、FPR、平均延迟和 P95 延迟。
- 安全风险分析报告与实验评测报告。

## 当前边界

- OpenCode Allow/Deny 执行前授权已接入；Ask 会等待 Dashboard 审批并在批准后继续原生调用。
- MCP Server 尚未作为当前 Git 提交的正式能力交付。
- `run_command` 已增加策略防绕过和资源限制，但不是容器或虚拟机级沙箱。
- SHA-256 哈希链可检测普通事后篡改；高保证部署还应增加 HMAC、数字签名或外部链头锚定。
- 待审批操作、审批结果、CT-TRM 污染来源和调用链摘要已持久化到 SQLite `data/state.db`。
- Trace 历史最多保留 500 条，当前没有归档、导出和按用户隔离功能。

## 运行验证

- 单元测试：104 项。
- 策略回归：100 条。
- CT-TRM benchmark：500 条主集 + 100 条 holdout 子集；保留旧 50 条回归集。
- Full CT-TRM 当前样本准确率：100%。
- Baseline 当前样本准确率：54%。
- 污染传播检测：17 条。
- 序列风险检测：12 条。
- 独立攻击场景：5 类。
- 无防护 ASR：100%。
- 防护后攻击成功率：0%。
- 防护阻断率：100%。
- 当前测试集 FPR：0%。
- 审计篡改：检测成功。
